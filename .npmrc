link-workspace-packages=true
# Allow all install scripts in Docker builds (pnpm v10 requirement)
enable-pre-post-scripts=true
# Allow native addon build scripts (pnpm v10 requires explicit approval)
allow-build=better-sqlite3,onnxruntime-node,keytar,sharp,esbuild,canvas
